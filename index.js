/**
 * Entry point — just points you to the demos.
 * Run `npm run demo:notify` or `npm run demo:treasury` directly instead.
 */

console.log(`
owockibot Agent Builder Starter Kit
------------------------------------
Available demos:

  npm run demo:notify      Bounty notification bot (watches for new bounties)
  npm run demo:treasury    Treasury tracker (reads pending Safe transactions)

See README.md for setup instructions and how to build your own bot on top of
src/api/bountyBoard.js and src/api/safeTreasury.js.
`);
